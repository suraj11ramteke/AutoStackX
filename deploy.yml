---
- hosts: all
  become: yes
  gather_facts: no
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  tasks:
    - name: Install required packages
      apt:
        name:
          - openjdk-17-jdk
          - nginx
          - postgresql
          - git
          - maven
          - nodejs
          - npm
          - zip
        update_cache: yes
        state: present
      register: apt_result
      until: apt_result is succeeded
      retries: 3
      delay: 10

    - name: Configure PostgreSQL for password authentication
      lineinfile:
        path: /etc/postgresql/16/main/pg_hba.conf
        regexp: '^local\s+all\s+postgres\s+peer$'
        line: 'local   all             postgres                                md5'
        state: present

    - name: Restart PostgreSQL service to apply authentication changes
      service:
        name: postgresql
        state: restarted

    - name: Create PostgreSQL database and user
      shell: |
        # Check if the database exists; if not, create it
        /usr/bin/psql -U postgres -tc "SELECT 1 FROM pg_database WHERE datname = 'testdb'" | grep -q 1 || /usr/bin/psql -U postgres -c "CREATE DATABASE testdb;"
        # Check if the user exists; if not, create it
        /usr/bin/psql -U postgres -tc "SELECT 1 FROM pg_roles WHERE rolname = 'testuser'" | grep -q 1 || /usr/bin/psql -U postgres -c "CREATE USER testuser WITH PASSWORD 'TestPass123';"
        # Grant privileges to the user on the database
        /usr/bin/psql -U postgres -c "GRANT ALL PRIVILEGES ON DATABASE testdb TO testuser;"
      environment:
        PGPASSWORD: 'Pass123'
      ignore_errors: yes

    - name: Add PostgreSQL user authentication method
      lineinfile:
        path: /etc/postgresql/16/main/pg_hba.conf
        line: "local   all             testuser                                md5"
        state: present

    - name: Reload PostgreSQL configuration
      shell: |
        /usr/lib/postgresql/16/bin/pg_ctl -D /etc/postgresql/16/main reload
      ignore_errors: yes

    - name: Clone Spring Boot app from GitHub
      git:
        repo: 'https://github.com/namdeopawar/Simple-SpringBoot-ReactApp.git'
        dest: /opt/SpringBootReactApp
        version: main

    - name: Run the Spring Boot application
      shell: |
        java -jar /opt/SpringBootReactApp/artifacts/myapp*.jar &
      async: 10
      poll: 0

    - name: Unzip frontend.zip to /var/www/html/myweb/
      unarchive:
        src: /opt/SpringBootReactApp/artifacts/frontend.zip
        dest: /var/www/html
        remote_src: yes

    - name: Configure Nginx
      template:
        src: nginx.conf.j2
        dest: /etc/nginx/sites-available/default

    - name: Test Nginx configuration
      command: nginx -t

    - name: Restart Nginx
      service:
        name: nginx
        state: restarted
